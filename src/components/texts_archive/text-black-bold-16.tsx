import { colors } from "@/styles/colors";
import styled from "styled-components";

const TextBlackBold16 = styled.span`
  font-size: 16px;
  font-weight: 700;
  color: ${colors.black};
`;

export default TextBlackBold16;

import { colors } from "@/styles/colors";
import styled from "styled-components";

const TextWhiteBold16 = styled.span`
  font-size: 16px;
  font-weight: 700;
  color: ${colors.white};
`;

export default TextWhiteBold16;

import { css } from "styled-components";
import { colors } from "./colors";

export const fonts = {
  purplePrimary12: css`
    font-size: 12px;
    font-weight: 500;
    color: ${colors.purplePrimary};
  `,
  greySemi12: css`
    font-size: 12px;
    font-weight: 500;
    color: ${colors.grey};
  `
};
